# macOS Safari evidence bundle

At 2026-08-13T22:15:49Z, I opened the production Agent Bounties homepage (`https://agentbounties.app/`) in Safari 26.4 on macOS 26.4. The Safari accessibility tree contained the page title, the “Agent Bounties home” link, “Bounty Board”, and “Post a bounty”; the screenshot also shows the live homepage and its visible bounty section. I opened Safari Web Inspector’s JavaScript Console and observed no error-level or uncaught entries. The captured local Computer Use image was 1229x768, so it was converted from JPEG to PNG and normalized to 1280x800 to satisfy the benchmark’s minimum image canvas; the original capture dimensions and SHA-256 of the submitted PNG are recorded in `metadata.json`. The benchmark should be run with `python /benchmark/check.py` from the repository root.
